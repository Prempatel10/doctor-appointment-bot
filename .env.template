# Secure Environment Configuration Template
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!

# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# Google Sheets Configuration  
GOOGLE_SHEETS_ID=your_google_sheets_id_here
GOOGLE_CREDENTIALS_FILE=credentials.json

# Google Service Account Credentials (JSON format as string)
# For Railway: Set as environment variable
# For local: Use credentials.json file or set this variable
GOOGLE_CREDENTIALS={"type":"service_account","project_id":"your_project_id",...}

# Security Notes:
# 1. Use strong, unique values for all tokens and IDs
# 2. For production (Railway), set these as environment variables
# 3. For local development, use encrypted files
# 4. Regularly rotate your credentials
# 5. Monitor access logs for unauthorized usage
